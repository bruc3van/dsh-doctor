对，第四个核心问题应当正式加入：

1. 如果暂时没有兼容版本，能否安全地临时禁用或删除插件，让 DSH 其余功能恢复？

重新设计后，`dsh-doctor` 不应只是“发现问题并尝试升级”，而应成为一个面向 DSH 升级事故的诊断与恢复决策工具。

# 一、产品目标重新定义

每次诊断必须按插件回答四个问题：

1. 哪个插件不兼容？
2. 哪一层配置造成或放大了不兼容？
3. 首选处理是升级插件、调整旧 patch，还是撤销配置覆盖？
4. 如果暂时修不好，能否临时隔离或彻底删除？会影响哪些其他功能？

最终不是输出一堆 finding，而是为每个插件生成一份“诊断与恢复决策”。

```
插件识别
  ↓
故障归因
  ↓
首选修复
  ↓
兼容版本可用性
  ↓
临时隔离
  ↓
移除影响评估
  ↓
用户选择动作
  ↓
重新诊断与恢复验证
```

# 二、核心数据模型

## 1. 插件诊断对象

建议每个插件使用以下结构：

```
{
  "name": "@mem9/dsh-plugin",
  "installedVersion": "0.1.0",
  "status": "incompatible",
  "cause": {
    "type": "plugin-version",
    "confidence": "confirmed",
    "summary": "插件声明的 DSH 兼容范围不包含当前版本"
  },
  "configuration": {
    "originLayer": "bundle",
    "entries": ["mem9"],
    "overriddenBy": [],
    "issues": []
  },
  "update": {
    "status": "not-checked"
  },
  "recovery": {
    "recommended": "update",
    "options": []
  }
}
```

其中 `status` 和 `recovery` 必须分离：

- `status` 描述现在是否兼容；
- `recovery` 描述用户有哪些处理选择；
- 不要因为“可以删除”就把插件视为已修复。

## 2. 故障原因

统一为以下类型：

```
plugin-version
plugin-artifact
client-contract
patch-version
config-override
duplicate-mount
missing-dependency
runtime-environment
unknown
```

例如：

- peer range 不匹配：`plugin-version`
- client bundle 缛失：`plugin-artifact`
- patch target 是旧 DSH entry：`patch-version`
- profile patch 覆盖核心 config：`config-override`
- bundle 和手工 insert 同时存在：`duplicate-mount`

`confidence`：

```
confirmed
likely
unknown
```

只有 `confirmed` 才能自动生成确定性修复；`likely/unknown` 只能提供诊断或隔离选择。

# 三、配置诊断设计

建立两个现场可重建的配置树：

```
currentDefaultTree
  = 当前 DSH installation + 当前 bundle layers

currentEffectiveTree
  = currentDefaultTree
  + profile patch
  + home patch
  + CLI overlays
```

并记录每个 entry 的字段来源：

```
{
  "id": "mem9",
  "name": "@mem9/dsh-plugin",
  "origin": {
    "kind": "bundle",
    "package": "@mem9/dsh-plugin",
    "file": "/package/cordis.patch.yml"
  },
  "fields": {
    "disabled": {
      "value": true,
      "source": {
        "kind": "profile",
        "file": "/Users/bruce/.dsh/profiles/web/cordis.patch.yml",
        "patchIndex": 3
      }
    },
    "config": {
      "source": {
        "kind": "profile",
        "patchIndex": 3
      },
      "replacedSource": {
        "kind": "bundle"
      },
      "removedPaths": ["enabled"]
    }
  }
}
```

优先增加这些 finding：

- `PATCH_INCOMPATIBLE_WITH_CURRENT_DSH`
- `DUPLICATE_ENTRY_ID`
- `DUPLICATE_PLUGIN_MOUNT`
- `CORE_ENTRY_DISABLED_BY_HIGHER_LAYER`
- `ENTRY_NAME_OVERRIDDEN`
- `ENTRY_STRUCTURE_OVERRIDDEN`
- `GROUP_CONTENT_REPLACED`
- `CONFIG_REPLACED_BY_HIGHER_LAYER`
- `BUNDLE_PROFILE_DECLARATION_CONFLICT`

历史 baseline 保留为增强能力，不作为基本诊断前提。

# 四、恢复决策模型

每个插件最多提供五种动作：

| 动作                | 用途                 | 是否修改状态                |
| ------------------- | -------------------- | --------------------------- |
| `update`            | 存在兼容候选版本     | 是                          |
| `adjust-patch`      | 配置仍针对旧版 DSH   | 是                          |
| `rollback-override` | 配置被覆盖           | 是                          |
| `quarantine`        | 暂时没有修复方案     | 默认只生成 overlay          |
| `remove`            | 用户决定不再保留插件 | 是，删除依赖和 bundle layer |

每个动作包含：

```
{
  "kind": "remove",
  "availability": "available",
  "risk": "medium",
  "recommended": false,
  "reason": "没有找到声明兼容当前 DSH 的发布版本",
  "impact": {},
  "command": []
}
```

`availability`：

```
available
unavailable
requires-review
unknown
```

# 五、判断是否存在兼容新版本

默认离线诊断不能声称“没有新版本”。

离线状态：

```
{
  "update": {
    "status": "not-checked"
  }
}
```

用户显式检查：

```
dsh-doctor diagnose --check-updates
```

或者进入恢复流程：

```
dsh-doctor recover @mem9/dsh-plugin
```

此时查询 registry：

1. 获取插件已发布版本；
2. 读取各版本 manifest；
3. 对比当前 DSH package versions 和 peer ranges；
4. 找出声明兼容的最高版本；
5. 不只看 `latest`，避免 latest 仍不兼容。

结果分为：

```
compatible-candidate-found
no-declared-compatible-version
metadata-insufficient
registry-unavailable
```

即使找到候选版本，也只能称为：

> manifest 声明兼容的候选版本

不能声称已经通过真实运行验证。

升级命令使用精确版本：

```
dsh plugin --profile web add @mem9/dsh-plugin@0.2.0
```

不建议模糊执行 latest update。

# 六、临时隔离设计

当没有兼容版本时，推荐顺序是：

```
先 quarantine 验证
    ↓
DSH 其余功能正常
    ↓
用户选择继续保持隔离，或彻底删除
```

## 1. 临时 quarantine

```
dsh-doctor recover @mem9/dsh-plugin --action quarantine
```

默认只显示计划。

显式生成：

```
dsh-doctor recover @mem9/dsh-plugin \
  --action quarantine \
  --output ./mem9-quarantine.yml
```

生成：

```
- id: mem9
  name: '@mem9/dsh-plugin'
  disabled: true
```

运行：

```
dsh --profile web --patch ./mem9-quarantine.yml
```

DSH 的 client module 系统只从存在 Fiber 且未 disabled 的 Loader entry 收集浏览器插件，因此正确禁用所有来源 entry 后，host 和 client 两侧都会退出组合。[index.ts](/Users/bruce/Documents/vscode-projects/deepseek-harness/packages/client/modules/src/index.ts)

## 2. quarantine 允许条件

只有满足以下条件才能生成：

- 插件对应的最终 entry 能精确定位；
- 所有 entry 都有非空 ID；
- 没有影响目标的重复 ID；
- name assertion 可确定；
- 插件所有活跃来源 entry 都能被禁用；
- bundle 主要是插入自己的 entry。

以下情况拒绝自动 quarantine：

- bundle 修改了大量已有核心 entry；
- package 到 entry 的映射不完整；
- 同一 client package 有多个无法全部禁用的来源；
- 插件提供其他插件必需的 Service，但依赖关系未知；
- 插件属于 DSH profile 模板的核心 bundle。

此时显示：

> 无法证明临时禁用不会破坏其他插件，请先人工审查影响。

## 3. 持久隔离

只有用户用临时 overlay 验证后，才允许：

```
dsh-doctor recover @mem9/dsh-plugin \
  --action persist-quarantine
```

持久化之前必须：

- 展示写入文件；
- 展示精确 YAML diff；
- 校验原文件 hash；
- 创建备份；
- 原子写入；
- 再次诊断。

持久隔离不是默认推荐，因为插件更新后可能已经恢复兼容，但旧的 `disabled: true` 会继续生效。

# 七、插件删除设计

删除必须是独立、显式选择，永远不能由通用 `--fix --yes` 推断。

```
dsh-doctor recover @mem9/dsh-plugin --action remove
```

## 1. 删除资格

允许删除的条件：

- 是 profile 的直接 dependency；
- 不是 DSH template/in-box 核心 bundle；
- 能解析到准确 package 名；
- 当前 DSH CLI 可用；
- profile manifest、patch 和 lockfile 可读；
- 已完成残留引用检查。

禁止删除：

- `@deepseek-ai/dsh-base` 等 profile 模板 bundle；
- 仅由其他 package 间接依赖的库；
- package 归属未知；
- profile manifest 已损坏；
- 删除后仍会留下无法禁用的手工 mount。

## 2. 删除影响预检

删除前输出：

```
{
  "package": "@mem9/dsh-plugin",
  "directDependency": true,
  "bundleLayer": true,
  "hostEntries": ["mem9"],
  "clientBundle": false,
  "manualMounts": [],
  "patchReferences": [],
  "clientDependents": [],
  "serviceDependents": {
    "status": "unknown"
  },
  "rollback": {
    "command": "dsh plugin --profile web add @mem9/dsh-plugin@0.1.0"
  }
}
```

重点检查：

- bundle layer 会否随依赖移除；
- profile/home patch 是否仍手工 insert 该插件；
- 是否还有 patch 引用其 entry ID；
- 其他插件的 client inject/external 是否依赖它；
- 是否提供其他插件需要的 Service；
- 是否存在多个 Loader source；
- 删除后是否会产生 dangling patch。

Service metadata 不完整时必须明确显示：

> 无法静态证明没有其他插件依赖该插件提供的运行时 Service。

不能写成“删除不会影响其他功能”。

## 3. 删除前处理残留配置

官方：

```
dsh plugin --profile web remove @mem9/dsh-plugin
```

会调用 pnpm，并在成功后根据安装状态把 dependency-managed bundle 从 `dsh.profile.bundles` 中移除。[plugin.ts](/Users/bruce/Documents/vscode-projects/deepseek-harness/apps/cli/src/plugin.ts)

但它不会自动清理用户手工写入的：

```
- insert:
    - id: mem9
      name: '@mem9/dsh-plugin'
```

因此 Doctor 必须先检查残留 mount。

如果存在：

- 能精确、安全修改：提供单独 YAML edit，先备份并移除手工 insert；
- 无法安全修改：拒绝自动删除，先生成 quarantine overlay；
- 绝不能先删除 package，再留下 enabled entry，否则下次启动会变成模块加载失败。

## 4. 删除执行顺序

安全顺序：

```
1. 保存诊断快照和原安装版本
2. 生成临时 quarantine
3. 检查手工 mount 和其他引用
4. 必要时先安全移除手工 mount
5. 执行官方 dsh plugin remove
6. 重新诊断
7. 检查 dependency、bundle、entry 和 client source 都已消失
8. 给出精确回滚安装命令
```

如果命令失败：

- 后续动作全部停止；
- 保留已有备份；
- 报告当前实际状态；
- 不声称删除完成。

## 5. 删除后的验证边界

Doctor 可以证明：

- package 已不再是 direct dependency；
- package 不再位于 bundle list；
- 有效配置中没有 enabled entry；
- client module 没有静态来源；
- patch 中没有 dangling target；
- 其余插件没有新增静态 blocking finding。

Doctor不能仅靠静态检查证明：

- 用户所有正常业务流程均可用；
- 没有动态 Service 依赖；
- 插件没有在外部位置留下数据；
- 真实 UI 已恢复。

因此删除后的结论应是：

> 静态配置和插件图已经恢复到无阻断状态；请重启该 profile 并验证主要功能。

官方也明确要求 bundle 添加、移除或更新后重启 profile。[README.zh.md](/Users/bruce/Documents/vscode-projects/deepseek-harness/apps/cli/reference/README.zh.md)

# 八、用户交互重新设计

不再让 `--fix` 把所有 repair 一次性执行。

推荐增加：

```
dsh-doctor diagnose
dsh-doctor recover <package>
dsh-doctor baseline create
```

交互示例：

```
插件：@mem9/dsh-plugin 0.1.0
状态：不兼容

原因：
  插件声明只兼容 DSH 0.1.0-rc.7
  当前 DSH 是 0.1.1-rc.2
  未发现 profile/home 配置覆盖该插件

兼容版本：
  未检查 registry

可选操作：
  1. 检查是否有兼容版本
  2. 生成临时隔离 overlay
  3. 评估删除影响
  4. 保持现状
```

检查后：

```
未找到声明兼容当前 DSH 的已发布版本。

建议：
  先生成临时隔离 overlay 并验证 DSH 其余功能。

后续选择：
  1. 保持临时隔离
  2. 持久禁用
  3. 删除插件
  4. 不处理
```

删除永远要求用户明确选择：

```
dsh-doctor recover @mem9/dsh-plugin --action remove --yes
```

`--fix --yes` 不能触发删除。

# 九、最终报告结构

按插件输出：

```
@mem9/dsh-plugin
  状态：不兼容
  原因：插件版本不支持当前 DSH
  配置层：bundle 原始配置，未发现更高层覆盖
  兼容版本：未找到声明兼容版本

  首选方案：
    暂时隔离并等待插件发布新版本

  临时隔离：
    可用
    将禁用 entry：mem9
    不修改安装状态

  删除：
    可用，但需要确认
    会移除：
      - profile dependency
      - bundle layer
      - host entry mem9
    未发现：
      - 手工 mount
      - client 依赖者
    未知：
      - 动态 Service 依赖

  回滚：
    dsh plugin --profile web add @mem9/dsh-plugin@0.1.0
```

JSON 完整保留所有证据和选项。

# 十、分阶段升级方案

## v0.2.0：升级后诊断核心

- current default/effective tree；
- 字段级 provenance；
- 插件与配置联合归因；
- 重复 mount、旧 patch、配置覆盖诊断；
- 新的 plugin diagnosis/recovery JSON 模型；
- 不增加删除和写操作。

## v0.2.1：兼容版本检查

- 可选 registry 查询；
- 查找声明兼容的精确版本；
- 区分“无兼容版本”“元数据不足”“网络失败”；
- 更新动作绑定精确版本。

## v0.3.0：临时隔离

- `recover <package> --action quarantine`；
- host/client entry 完整来源检查；
- 生成临时 overlay；
- 重新组合验证；
- 不自动持久化。

## v0.3.1：安全删除

- removal impact；
- 核心 bundle 保护；
- 手工 mount 和 dangling patch 检查；
- 通过当前 DSH CLI 执行官方 remove；
- 记录原版本和回滚命令；
- 删除后完整重诊断。

## v0.4.0：历史基线与持久恢复

- baseline create/compare；
- 升级前后差异归因；
- 持久 quarantine；
- 经确认的 YAML 精确编辑；
- 完整备份和恢复流程。

这套重构把“删除插件”作为正式恢复路径，但遵循一个重要原则：

> 没有兼容版本时，默认先隔离验证；确认 DSH 其余功能正常后，再由用户决定保持隔离还是删除。删除必须显式选择、完成影响预检，并保留精确回滚路径。

本次仅重新构建设计方案，没有修改代码或用户配置。